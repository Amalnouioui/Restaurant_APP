<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Receipt #{{ commande.id }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'DejaVu Sans Mono', 'Courier New', monospace;
            width: 80mm;
            margin: 0 auto;
            padding: 10px;
            background: white;
            font-size: 11px;
        }
        
        .receipt {
            border: 2px dashed #333;
            padding: 15px;
        }
        
        .header {
            text-align: center;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
            margin-bottom: 10px;
        }
        
        .header h1 {
            font-size: 20px;
            margin-bottom: 5px;
        }
        
        .header p {
            font-size: 10px;
            margin: 2px 0;
        }
        
        .order-info {
            margin-bottom: 10px;
            padding-bottom: 10px;
            border-bottom: 1px dashed #333;
        }
        
        .order-info div {
            display: flex;
            justify-content: space-between;
            margin: 4px 0;
            font-size: 10px;
        }
        
        .items {
            margin-bottom: 10px;
        }
        
        .items h3 {
            text-align: center;
            font-size: 13px;
            margin-bottom: 8px;
            text-transform: uppercase;
        }
        
        .item {
            display: flex;
            justify-content: space-between;
            margin: 6px 0;
            font-size: 10px;
        }
        
        .item-name {
            flex: 1;
        }
        
        .item-qty {
            width: 35px;
            text-align: center;
        }
        
        .item-price {
            width: 55px;
            text-align: right;
        }
        
        .item-detail {
            font-size: 9px;
            color: #666;
            margin-left: 10px;
        }
        
        .totals {
            padding-top: 8px;
            border-top: 2px solid #333;
        }
        
        .total-row {
            display: flex;
            justify-content: space-between;
            margin: 4px 0;
            font-size: 10px;
        }
        
        .total-row.grand-total {
            font-size: 15px;
            font-weight: bold;
            margin-top: 8px;
            padding-top: 8px;
            border-top: 2px solid #333;
        }
        
        .payment-status {
            text-align: center;
            margin: 10px 0;
            padding: 8px;
            background: #f0f0f0;
            border: 1px solid #333;
            font-size: 10px;
        }
        
        .payment-status.paid {
            background: #d4edda;
            color: #155724;
        }
        
        .payment-status.pending {
            background: #fff3cd;
            color: #856404;
        }
        
        .footer {
            text-align: center;
            margin-top: 15px;
            padding-top: 10px;
            border-top: 2px dashed #333;
            font-size: 9px;
        }
        
        .footer p {
            margin: 4px 0;
        }
        
        .barcode {
            text-align: center;
            font-size: 22px;
            letter-spacing: 2px;
            margin: 10px 0;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="receipt">
        <div class="header">
            <h1>RESTAURANT</h1>
            <p>123 Main Street</p>
            <p>Tel: (555) 123-4567</p>
            <p>www.restaurant.local</p>
        </div>
        
        <div class="order-info">
            <div>
                <strong>Order #:</strong>
                <span>{{ commande.id }}</span>
            </div>
            <div>
                <strong>Date:</strong>
                <span>{{ commande.dateHeure|date('m/d/Y') }}</span>
            </div>
            <div>
                <strong>Time:</strong>
                <span>{{ commande.dateHeure|date('H:i') }}</span>
            </div>
            <div>
                <strong>Cashier:</strong>
                <span>Walk-In POS</span>
            </div>
        </div>
        
        <div class="items">
            <h3>ITEMS</h3>
            {% for ligne in commande.ligneCommandes %}
                <div class="item">
                    <div class="item-name">{{ ligne.plat.nomPlat }}</div>
                    <div class="item-qty">x{{ ligne.quantite }}</div>
                    <div class="item-price">${{ (ligne.plat.prix * ligne.quantite)|number_format(2, '.', ',') }}</div>
                </div>
                <div class="item-detail">
                    @ ${{ ligne.plat.prix|number_format(2, '.', ',') }} each
                </div>
            {% endfor %}
        </div>
        
        <div class="totals">
            {% set subtotal = 0 %}
            {% for ligne in commande.ligneCommandes %}
                {% set subtotal = subtotal + (ligne.plat.prix * ligne.quantite) %}
            {% endfor %}
            {% set tax = subtotal * 0.10 %}
            {% set service = 5.00 %}
            
            <div class="total-row">
                <span>Subtotal:</span>
                <span>${{ subtotal|number_format(2, '.', ',') }}</span>
            </div>
            <div class="total-row">
                <span>Tax (10%):</span>
                <span>${{ tax|number_format(2, '.', ',') }}</span>
            </div>
            <div class="total-row">
                <span>Service:</span>
                <span>${{ service|number_format(2, '.', ',') }}</span>
            </div>
            <div class="total-row grand-total">
                <span>TOTAL:</span>
                <span>${{ commande.total|number_format(2, '.', ',') }}</span>
            </div>
        </div>
        
        <div class="payment-status {{ commande.paymentStatus == 'PAID' ? 'paid' : 'pending' }}">
            {% if commande.paymentMethod == 'CARD' %}
                <strong>PAID - CARD</strong><br>
                Payment Status: {{ commande.paymentStatus }}
            {% else %}
                <strong>CASH PAYMENT</strong><br>
                Status: {{ commande.paymentStatus }}
            {% endif %}
        </div>
        
        <div class="barcode">
            *{{ commande.id }}*
        </div>
        
        <div class="footer">
            <p><strong>Thank you for your order!</strong></p>
            <p>Please keep this receipt for your records</p>
            {% if commande.paymentMethod == 'CASH' and commande.paymentStatus == 'AWAITING_CASH' %}
                <p style="margin-top: 8px; font-weight: bold;">
                    Please pay at the counter
                </p>
            {% endif %}
            <p style="margin-top: 12px; font-size: 8px;">
                Printed: {{ "now"|date('m/d/Y H:i') }}
            </p>
        </div>
    </div>
</body>
</html>
